<project>
  <!-- Using environment varibles with ${env.<value>} -->
  <property environment="env"/>

  <!-- product properties -->
  <property file="product.properties"/>

  <property name="buildDir"  location="D:\jonmag docs\Magnus\Java\BuildDir\${ant.project.name}"/>
  <property name="sourceDir" location="${basedir}/src" />
  <property name="jarfile"   location="${buildDir}/${ant.project.name}.jar" />
  <property name="outputDir" location="${buildDir}/classes" />

	<property name="resourceDir"   location="${basedir}\resources"/>
  <property name="deploymentDir" location="${basedir}\deployment"/>
  <property name="3ppJars"       location="D:\jonmag docs\Magnus\Java\3ppJars"/> 
  <property name="buildSupport"  location="D:\jonmag docs\Magnus\Java\TimeTool\buildSupport"/> 

  <target name="setup" description="Create the necessary directories">
    <mkdir dir="${buildDir}" />
    <mkdir dir="${outputDir}" />
  </target>

  <target name="clean">
    <delete dir="${buildDir}"/>
  </target>
</project>
